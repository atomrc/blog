/*global define*/

define([],
    function () {
        'use strict';
        var services = {

            application: function () {
                return {
                    'apikey' : null
                };
            }

        };

        return services;
    }
);

