ul
    li(ng-repeat='post in posts')
        a(href="/#/posts/{{ post.slug }}")
                h2 {{ post.title }}
                div.pubdate {{ post.pubdate }}
                p.description {{ post.description }}
