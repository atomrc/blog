- if (auth)
    include _content
    form(ng-submit='save()', draggable)
        fieldset
            input(type='text', data-ng-model='post.title')
            br
            textarea(data-ng-model='post.description')
            br
            textarea(data-ng-model='post.body')
            br
            input(type='submit')
