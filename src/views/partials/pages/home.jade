.section-title Articles
    .progress.small(data-ng-show="!posts.$resolved") loading
    - if (auth)
        form(data-ng-submit="create(newPost); newPost = null;")
            input(data-ng-model="newPost.title")
            button Create
hr
#posts-container(data-posts-container="")

//.section-title Talks
//#talks-container(data-talks-container="")
